# bridgekit

Monorepo for the bridgekit library and CLI.

## Packages

- `packages/core` — `@malopezr7/bridgekit`: Typed bidirectional bridge between React Native and native (Android/iOS).
- `packages/cli` — `@malopezr7/bridgekit-cli`: Code generation CLI for bridgekit contracts.
